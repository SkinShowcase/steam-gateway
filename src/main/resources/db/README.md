# steam-gateway: без БД

Сервис steam-gateway не имеет собственной БД (принцип единственной ответственности).
Он только запрашивает данные у lis-skins и отдаёт JSON сервису items, который обрабатывает и сохраняет данные в своей БД.
